# MCP config may contain a GitHub PAT — do not commit
.cursor/mcp.json

# Python
.venv/
__pycache__/
*.egg-info/

# Durable file storage (api.storage_root default)
.data/

# Frontend (packages/frontend has its own .gitignore for node_modules, dist)
