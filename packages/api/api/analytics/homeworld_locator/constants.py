"""Homeworld locator analytic identity."""

ANALYTIC_ID = "homeworld-locator"

ATTRIBUTION_INFERRED = "inferred"
ATTRIBUTION_USER_ASSERTED = "user_asserted"

# Bump when layout-prior cost, caps, stand-in policy, tie-break, or the candidate
# set feeding selection (promote/cull) changes in a way that stale shell selections
# must not be reused.
LAYOUT_PRIOR_ALGORITHM_VERSION = 2
