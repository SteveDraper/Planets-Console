# Inference prior miner discovery config for campaign games (#92).
# Campaign: settings.campaignmode true (GameCategory.from_game_settings).
# Authoritative spec: docs/design-military-score-inference-build-priors.md section 10.1
version: 1
patterns:
  # v1 seed pattern: small cap until loadall + mining pipeline is validated in CI/dev.
  - id: campaign-v1-seed
    game_category: campaign
    max_games: 50
    min_difficulty: 1.0
    earliest_date: "2024-01-01"
